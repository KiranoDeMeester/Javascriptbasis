// ========================= features/ui/renderer.js =========================
export function renderGame() {
// Basic starting point — intended for expansion
    const app = document.getElementById('app');
    app.innerHTML = '<h1>Solitaire</h1>';
}